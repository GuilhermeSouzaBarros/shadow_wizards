Criar ambiente virutal:
    sudo apt install python3.12-venv
    python3.12 -m venv <Nome que tu quiser>
    source <Nome que tu quiser>/bin/activate

Instalar imports:
    pip install -r imports.txt
